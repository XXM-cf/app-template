export const SET_TOKEN = (state, token) => {
  state.token = token
}
export const SET_PERMISSIONS = (state, permissions) => {
  state.permissions = permissions
}
export const SET_USER = (state, user) => {
  state.user = user
}

export const SET_CLIENTID = (state, clientIds) => {
  state.clientIds = clientIds
}

export const SET_POSITIONS = (state, positions) => {
  state.positions = positions
}

export const SET_WEATHERS = (state, weathers) => {
  state.weathers = weathers
}
